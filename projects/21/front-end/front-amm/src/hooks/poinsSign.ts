import { calculateGasMargin } from 'utils'
import { BigNumber } from '@ethersproject/bignumber'
import { usePoinsContract } from './useContract'

export default async function Poinssign(account:any) {
  if (!account) return;
  return 1000
}
